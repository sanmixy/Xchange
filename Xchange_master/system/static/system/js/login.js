/**
 * Created by Xnus on 16-12-4.
 */
$(function () {
    var $global_login_area = $('div#global_login_are');
    $global_login_area.css('width', window.outerWidth);
    $global_login_area.css('height', window.outerHeight);
});
